import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    flex: 1,
    marginRight: 7, 
  },
  buttonText: {
    fontSize: 12,
    fontWeight: '500',
    color: '#4076D9', 
    textAlign: 'right',
  },
});

export default styles;
